# Dynamic Image Authentication

A small static web app where a user selects or randomly chooses a favorite color and favorite animal, generates a visual challenge, and authenticates by clicking the matching full-body pictogram-style animal silhouette in the image. A wrong click automatically resets the challenge, and each challenge has a 20-second time limit.

## Run

Open `index.html` in a browser.

No build step or server is required.
